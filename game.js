function gamePlay () {
    var quit = false //decalred a variable to track if player has quit game
    var start //declared a variable to initiat game
    console.log('Lets get it started')
    if (start = 'y') {
        while (quit == false){
            quit = true
        }
    } else {
        console.log('I see... there is no room for the weakhearted.')
    }

}

gamePlay()